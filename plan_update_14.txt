If FECHA INICIAL INTERES is 15-06-2026, and the user expects a payment on 15-06-2026, then `FECHA INICIAL INTERES` IS A PAYMENT DATE! It's not just the accrual start date. The calendar generator should INCLUDE `start_date` as the first payment date!
Wait, but if 15-06-2026 is the first payment date, when did it start accruing?
In the interest engine:
```python
    start_date = pd.to_datetime(combined_row.get('FECHA INICIAL INTERES'), errors='coerce')
    if pd.isna(start_date):
        start_date = interest_dates[0] - pd.DateOffset(months=months_step)
```
Wait, if `interest_dates[0]` is `15-06-2026`, and `start_date` (which is accrual start) is ALSO `15-06-2026`, then `current_start >= current_date` will be true, and it will do:
`current_start = current_date - pd.DateOffset(months=6)`!
So it will accrue from 15-12-2025! Which makes perfect sense, because `FECHA FIRMA` in inventario is 2025-12-11. So the accrual actually starts 6 months before the first payment.
Therefore, `FECHA INICIAL INTERES` actually means the FIRST PAYMENT DATE for interests!
Let me fix `modules/calendar_generator.py` to start appending dates from `start_date` directly!
