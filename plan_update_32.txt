Wait, the user is saying 542100106 is not generating interests.
And my flow shows `pago_interes` is `0.0`.
Why is `pago_interes` 0.0?
Because `generate_interest_calendar` relies on `FECHA FINAL INTERES` from Guias.
Let's look at the Guias data for 542100106:
`[{'CREDITO': 542100106, 'FECHA INICIAL INTERES': '15-08-2010', 'FECHA FINAL INTERES': '15-08-2020'...}`
`{'CREDITO': 542100106, 'FECHA INICIAL INTERES': '15-02-2021', 'FECHA FINAL INTERES': '15-02-2037'...}]`

Notice there are TWO rows in `proy_consulta_guias` for this credit!
In `main.py`, I did:
```python
if isinstance(guias_row, pd.DataFrame): guias_row = guias_row.iloc[0]
```
So it takes the *first* row!
The first row has `FECHA FINAL INTERES = '15-08-2020'`.
Since 2020 is BEFORE the cutoff date of `2026-04-30`, the interest calendar is EMPTY!
Thus, NO INTEREST is generated.

But wait! `proy_consulta_guias` can have multiple rows (called "Guias").
`row_guias` actually contains a sequence of guias!
If there are multiple rows for the same credit, I should probably generate the calendar taking the active guías (e.g. `FECHA FINAL INTERES` > cutoff_date), OR just generate the calendar for all Guías and combine them!
Let's see if we should combine them or just pick the latest one or the one where `FECHA FINAL INTERES` >= cutoff_date.
If I just pick the row where `FECHA FINAL INTERES` >= CUTOFF_DATE, it will solve it perfectly.
Or better yet, `guias_lookup` should probably contain ALL rows, and `interest_engine` should process them. But to keep it simple and correct, I can just filter `df_guias` to only keep rows where `FECHA FINAL INTERES >= CUTOFF_DATE` or take the `max` FECHA FINAL INTERES. Or in `main.py`:
When `guias_row` is a DataFrame (multiple rows), pick the one where `FECHA FINAL INTERES` >= `CUTOFF_DATE`! If none, just take the last one.
