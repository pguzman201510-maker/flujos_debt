The user wants the terminal to print a list of credits that have `TIPO AMORTIZACION = 'ND'` but are NOT found in the `tabla_nd.xlsx` file.

Currently, the system already warns about this in `calendar_generator.py` line 133:
`logger.warning(f"Credit {credito_id}: Type ND but not found in tabla ND. Assuming BULLET at ULT_PAGO.")`
But maybe they want a consolidated summary or a specific list printed out at the end, or a clearer list instead of just intermingled logs.

I should:
1. Update `main.py` or `calendar_generator.py` to collect the list of these missing ND credits.
2. Print the list clearly at the end of the script execution.

Let's do it in `main.py` where we can track them.
Wait, `calendar_generator` already checks it, but `main` is where we orchestrate. We can track missing ND credits globally and print them.
